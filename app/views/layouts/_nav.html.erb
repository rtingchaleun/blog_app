<nav class="navbar navbar-expand-md navbar-light fixed-top bg-white border-bottom">
  <%= link_to 'My Blog App', root_path, class: 'navbar-brand' %>

  <%= form_for(posts_path, method: :get, :html => {class: 'form-inline'}) do |f| %>
    <div class="input-group">
      <%= text_field_tag(:q, params[:q], class: 'form-control') %>
      <div class="input-group-append">
        <%= submit_tag('Search', name: nil, class: 'btn btn-outline-success') %>
      </div>
    </div>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarCollapse">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item mx-2">
        <%= link_to 'My Posts', my_posts_path, class: 'nav-link' %>
      </li>
      <li class="nav-item mx-2">
        <%= link_to 'New Post', new_post_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <% if user_signed_in?  %>
          <%= link_to 'Log out', destroy_user_session_path, class: 'btn btn-outline-secondary ml-2', method: :delete %>
        <% else %>
          <%= link_to 'Log in', new_user_session_path, class: 'btn btn-outline-secondary' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
